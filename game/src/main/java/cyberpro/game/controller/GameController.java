package cyberpro.game.controller;

public class GameController {
	// main menu: enter number of players, enter players' names, choose players' colors, choose a map, then start
	
	/*
	 * create a game:
	 * Players list
	 * Modifiers initialization,
	 * Board, initialize Board,
	 * start the game
	 */
	
	// game process
		
		// Draw the objects
	
		// Start receiving input commands from GUI
	
		// few times a second repeat the same actions:
			// process players' keyboard commands
			// cycle over all objects and check if any actions needed with them, if yes - make them
}
